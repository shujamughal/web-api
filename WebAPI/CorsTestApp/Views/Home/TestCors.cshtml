@section Scripts {
<script>
    async function fetchProducts() {
        try {
            const response = await fetch("https://localhost:7103/api/products"); // Replace with your API URL
            if (!response.ok) {
                throw new Error(`HTTP error! status: ${response.status}`);
            }
            const data = await response.json();
            console.log("Products:", data);
            document.getElementById("results").innerText = JSON.stringify(data, null, 2);
        } catch (error) {
            console.error("Error fetching products:", error);
            document.getElementById("results").innerText = error.message;
        }
    }
    

    // Call the function when the page loads
    document.addEventListener("DOMContentLoaded", fetchProducts);
</script>
}
    <h2>Test CORS Implementation</h2>
    <p>Below is the result of the cross-origin request:</p>
    <pre id="results">Loading...</pre>